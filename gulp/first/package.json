{
  "name": "first",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mini": "rimraf dist && gulp minifyJS --name",
    "copy": "rimraf dist && gulp copy --name"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-load-plugins": "^2.0.7",
    "gulp-uglify": "^3.0.2",
    "minimist": "^1.2.8",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1"
  }
}

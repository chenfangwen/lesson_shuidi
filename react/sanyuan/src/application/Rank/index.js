import React from 'react';
// import { renderRoutes } from 'react-router-config'

function Rank(props){
    // console.log(props)
    // const {route} = props
    return (
        <div>
            Rank
        </div>
    );
}
 
export default React.memo(Rank);
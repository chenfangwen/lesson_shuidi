function LwstDP (a,b,n,m) {

    for(var i=0;i<5;i++){        //一维长度为3
        A[i] = new Array();
        A[0][i] = i
        A[i][0] = i
    }
    console.log(A)
    // for(let i = 0; i < n; i++){ 

    // }
}

LwstDP('asd','sds',3,3)